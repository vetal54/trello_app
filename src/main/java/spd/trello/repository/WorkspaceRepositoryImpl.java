package spd.trello.repository;

import org.springframework.stereotype.Repository;
import spd.trello.entity.resource.Workspace;

@Repository
public interface WorkspaceRepositoryImpl extends AbstractRepository<Workspace> {
}
