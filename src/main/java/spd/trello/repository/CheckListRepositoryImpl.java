package spd.trello.repository;

import org.springframework.stereotype.Repository;
import spd.trello.entity.resource.CheckList;

@Repository
public interface CheckListRepositoryImpl extends AbstractRepository<CheckList> {
}
